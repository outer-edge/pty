---
// ChromaticHero: Ultra-subtle prismatic effect for hero headline
// Three.js chromatic aberration - rare, quiet, expensive
---

<div class="chromatic-hero-wrapper">
  <h1 class="hero-headline" id="hero-headline">
    <slot />
  </h1>
  <canvas id="chromatic-canvas" class="chromatic-canvas" aria-hidden="true"></canvas>
</div>

<style>
  .chromatic-hero-wrapper {
    position: relative;
    max-width: 960px;
    padding-top: 48px;
  }

  .hero-headline {
    font-family: var(--font-grotesk);
    font-size: clamp(48px, 8vw, 96px);
    font-weight: 400;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin-bottom: 32px;
    position: relative;
    z-index: 1;
  }

  .chromatic-canvas {
    position: absolute;
    top: 48px;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 2;
  }

  @media (prefers-reduced-motion: reduce) {
    .chromatic-canvas {
      display: none;
    }
  }

  @media (max-width: 968px) {
    .chromatic-hero-wrapper {
      padding-top: 32px;
    }
    
    .chromatic-canvas {
      top: 32px;
      opacity: 0 !important;
    }
  }
</style>

<script>
  // Lazy-load Three.js and initialize prismatic effect
  async function initChromaticEffect() {
    // Check for reduced motion preference
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
      return;
    }

    // Skip on mobile for subtlety
    if (window.innerWidth <= 968) {
      return;
    }

    const canvas = document.getElementById('chromatic-canvas') as HTMLCanvasElement;
    const headline = document.getElementById('hero-headline') as HTMLElement;
    
    if (!canvas || !headline) return;

    // Lazy-load Three.js
    const THREE = await import('three');

    // Capture headline text as texture
    function captureTextTexture() {
      const rect = headline.getBoundingClientRect();
      const styles = window.getComputedStyle(headline);
      
      // Create temporary canvas to render text
      const textCanvas = document.createElement('canvas');
      const dpr = Math.min(window.devicePixelRatio, 2);
      textCanvas.width = rect.width * dpr;
      textCanvas.height = rect.height * dpr;
      const ctx = textCanvas.getContext('2d')!;
      
      ctx.scale(dpr, dpr);
      ctx.clearRect(0, 0, rect.width, rect.height);
      
      // Apply computed styles
      ctx.font = `${styles.fontWeight} ${styles.fontSize} ${styles.fontFamily}`;
      ctx.fillStyle = styles.color;
      ctx.textBaseline = 'top';
      ctx.letterSpacing = styles.letterSpacing;
      
      // Render text with proper line wrapping
      const text = headline.textContent || '';
      const words = text.split(' ');
      const lineHeight = parseFloat(styles.fontSize) * parseFloat(styles.lineHeight);
      let line = '';
      let y = 0;
      
      for (let i = 0; i < words.length; i++) {
        const testLine = line + words[i] + ' ';
        const metrics = ctx.measureText(testLine);
        
        if (metrics.width > rect.width && i > 0) {
          ctx.fillText(line, 0, y);
          line = words[i] + ' ';
          y += lineHeight;
        } else {
          line = testLine;
        }
      }
      ctx.fillText(line, 0, y);
      
      return textCanvas;
    }

    const textCanvas = captureTextTexture();
    const texture = new THREE.CanvasTexture(textCanvas);
    texture.needsUpdate = true;

    // Setup Three.js
    const scene = new THREE.Scene();
    const camera = new THREE.OrthographicCamera(-1, 1, 1, -1, 0, 1);
    const renderer = new THREE.WebGLRenderer({ 
      canvas, 
      alpha: true,
      antialias: true,
      powerPreference: 'low-power'
    });

    const rect = headline.getBoundingClientRect();
    renderer.setSize(rect.width, rect.height);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

    // Chromatic aberration shader - samples actual text texture
    const material = new THREE.ShaderMaterial({
      transparent: true,
      uniforms: {
        tDiffuse: { value: texture },
        uStrength: { value: 0.0 },
        uResolution: { value: new THREE.Vector2(rect.width, rect.height) }
      },
      vertexShader: `
        varying vec2 vUv;
        void main() {
          vUv = uv;
          gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
        }
      `,
      fragmentShader: `
        uniform sampler2D tDiffuse;
        uniform float uStrength;
        uniform vec2 uResolution;
        varying vec2 vUv;
        
        void main() {
          vec2 uv = vUv;
          
          // Sample the text texture with RGB offsets (chromatic aberration)
          float offset = uStrength * 0.002; // Very subtle offset
          
          // Offset each color channel slightly
          float r = texture2D(tDiffuse, uv + vec2(offset, offset * 0.5)).a;
          float g = texture2D(tDiffuse, uv).a;
          float b = texture2D(tDiffuse, uv - vec2(offset, offset * 0.5)).a;
          
          // Get the original color at center
          vec4 centerColor = texture2D(tDiffuse, uv);
          
          // Only show chromatic fringe at edges where alpha transitions
          vec2 texelSize = 1.0 / uResolution;
          float alphaL = texture2D(tDiffuse, uv - vec2(texelSize.x, 0.0)).a;
          float alphaR = texture2D(tDiffuse, uv + vec2(texelSize.x, 0.0)).a;
          float alphaT = texture2D(tDiffuse, uv + vec2(0.0, texelSize.y)).a;
          float alphaB = texture2D(tDiffuse, uv - vec2(0.0, texelSize.y)).a;
          
          // Edge detection - where alpha changes
          float edgeFactor = abs(alphaL - alphaR) + abs(alphaT - alphaB);
          edgeFactor = smoothstep(0.1, 0.3, edgeFactor);
          
          // Build the chromatic aberration color
          // Cyan/magenta/amber hints on the edges
          vec3 chromaticColor = vec3(
            r * 0.8 + g * 0.1 + b * 0.3,  // Reddish/amber
            r * 0.2 + g * 0.9 + b * 0.5,  // Greenish/cyan
            r * 0.3 + g * 0.4 + b * 1.0   // Bluish/magenta
          );
          
          // Mix original text color with chromatic fringe at edges
          vec3 finalColor = mix(
            centerColor.rgb,
            chromaticColor,
            edgeFactor * uStrength
          );
          
          // Alpha is based on text presence + fringe
          float finalAlpha = max(centerColor.a, edgeFactor * uStrength * 0.6);
          
          gl_FragColor = vec4(finalColor, finalAlpha);
        }
      `
    });

    const geometry = new THREE.PlaneGeometry(2, 2);
    const mesh = new THREE.Mesh(geometry, material);
    scene.add(mesh);

    // Animation state
    let isAnimating = false;
    let animationStartTime = 0;
    let animationDuration = 0;
    let animationFrame: number | null = null;
    let nextTwitchTimeout: ReturnType<typeof setTimeout> | null = null;

    // Trigger effect
    function triggerEffect() {
      if (isAnimating || document.hidden || window.innerWidth <= 968) return;
      
      isAnimating = true;
      animationStartTime = Date.now();
      // Random duration 200-400ms
      animationDuration = 200 + Math.random() * 200;
      
      // Start render loop only during twitch
      renderLoop();
    }

    // Render loop - only runs during active twitch
    function renderLoop() {
      const now = Date.now();
      const elapsed = now - animationStartTime;

      if (isAnimating && elapsed < animationDuration) {
        // Ease in-out for smooth twitch
        const progress = elapsed / animationDuration;
        const eased = progress < 0.5 
          ? 2 * progress * progress 
          : 1 - Math.pow(-2 * progress + 2, 2) / 2;
        
        const currentStrength = 1.0 * (1 - eased);
        material.uniforms.uStrength.value = currentStrength;
        
        renderer.render(scene, camera);
        animationFrame = requestAnimationFrame(renderLoop);
      } else {
        // Twitch complete
        isAnimating = false;
        material.uniforms.uStrength.value = 0.0;
        renderer.render(scene, camera); // One final clear frame
        animationFrame = null;
        
        // Schedule next twitch (6-12s random)
        const nextDelay = 6000 + Math.random() * 6000;
        nextTwitchTimeout = setTimeout(triggerEffect, nextDelay);
      }
    }

    // Handle window resize
    let resizeTimeout: ReturnType<typeof setTimeout>;
    let isDisposed = false;
    
    function handleResize() {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        // Dispose on mobile
        if (window.innerWidth <= 968 && !isDisposed) {
          cleanup();
          return;
        }
        
        if (isDisposed) return;
        
        // Recreate texture with new size
        const newTextCanvas = captureTextTexture();
        texture.image = newTextCanvas;
        texture.needsUpdate = true;
        
        const newRect = headline.getBoundingClientRect();
        renderer.setSize(newRect.width, newRect.height);
        material.uniforms.uResolution.value.set(newRect.width, newRect.height);
        
        // Render once to update display
        if (!isAnimating) {
          renderer.render(scene, camera);
        }
      }, 250);
    }

    function cleanup() {
      isDisposed = true;
      if (animationFrame) cancelAnimationFrame(animationFrame);
      if (nextTwitchTimeout) clearTimeout(nextTwitchTimeout);
      window.removeEventListener('resize', handleResize);
      renderer.dispose();
      geometry.dispose();
      material.dispose();
      texture.dispose();
    }

    window.addEventListener('resize', handleResize);

    // Initial render (idle state)
    renderer.render(scene, camera);

    // Trigger first effect after short delay (2-4s random)
    nextTwitchTimeout = setTimeout(triggerEffect, 2000 + Math.random() * 2000);

    return cleanup;
  }

  // Initialize when page loads
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initChromaticEffect);
  } else {
    initChromaticEffect();
  }
</script>
