---
// Crystal-filled middle word - photoreal glass texture INSIDE glyphs only
// NO rectangular wash, NO neon spectrum - CSS background-clip:text baseline
const { word } = Astro.props;
---

<div class="crystal-word-container">
  <h1 class="crystal-word">{word}</h1>
</div>

<style>
  .crystal-word-container {
    position: relative;
    width: fit-content;
    margin: 0 auto;
  }

  .crystal-word {
    position: relative;
    z-index: 1;
    font-family: 'Anton', 'Impact', sans-serif;
    font-size: clamp(96px, 22vw, 380px);
    font-weight: 400;
    line-height: 0.85;
    letter-spacing: 0.02em;
    margin: 0;
    text-transform: uppercase;
    /* Glass texture INSIDE glyphs ONLY - NO rectangular wash */
    background-image: url('/textures/crystal-fill.png');
    background-size: 700px 700px;
    background-position: center;
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
  }

  @media (max-width: 768px) {
    .crystal-word {
      /* Same glass texture on mobile - glyph-masked */
      background-image: url('/textures/crystal-fill.png');
      background-size: 500px 500px;
      background-position: center;
      background-clip: text;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .crystal-word {
      background-image: url('/textures/crystal-fill.png');
      background-size: 650px 650px;
      background-position: center;
      background-clip: text;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
  }
</style>
